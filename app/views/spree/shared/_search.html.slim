= form_tag spree.products_path, method: :get do
  = search_field_tag(:keywords,
                     params[:keywords],
                     id: "search-field", class: "mdl-textfield__input",
                     placeholder: Spree.t(:search))
